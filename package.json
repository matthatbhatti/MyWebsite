{
  "name": "project",
  "version": "1.0.0",
  "description": "A Node.js application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "if exist build rmdir /s /q build && mkdir build && copy project.html build\\index.html && copy *.css build\\ && copy *.js build\\ && copy *.jpg build\\ && copy *.png build\\ && copy *.webp build\\ && copy *.gif build\\ && copy *.svg build\\",
    "web": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
